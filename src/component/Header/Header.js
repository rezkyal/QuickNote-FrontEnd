import React from 'react';
import "./Header.scss"

const Header = props =>{
    return(
        <header className="Header">
            <p className="TitleFont">
                QuikNote
            </p>
        </header>
    )
}

export default Header